import React from 'react';
import { FormattedMessage } from 'react-intl';

const Home = () => (
    <h2>
        <FormattedMessage
            defaultMessage="Home"
            id="home.title"
        />
    </h2>
);

export default Home;
